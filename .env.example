# Never commit .env.local.
# Local development (Vite dev proxy) and production (Netlify/Vercel serverless function):
# Do NOT use a VITE_ prefix here — that would embed the key in the client bundle.
FD_API_KEY=your_football_data_api_key_here
VITE_FD_BASE_URL=/api